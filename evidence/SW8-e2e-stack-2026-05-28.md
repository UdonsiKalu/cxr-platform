# E2E stack evidence — Terraform + Helm + Argo + CXR UI

**Date:** 2026-05-28  
**Cluster:** `kind-cxr-lab`  
**Git:** https://github.com/UdonsiKalu/cxr-ops-lab (`helm/cxr-ui`)

## Flow

1. `terraform apply` → `null_resource.kind_cluster` (SW.5)
2. `12-k8-ensure.sh` → `cxr-ui:local` loaded + Helm release `cxr-ui` (SW.3–4)
3. `13-argo-install.sh` → Argo CD + Application `cxr-ui` from Git (SW.8)
4. `14-stack-test.sh` → **ALL CHECKS PASSED**

## Argo Application

```
sync=Synced health=Healthy
```

## URLs (port-forward)

| Service | URL |
|---------|-----|
| CXR UI | http://localhost:8081 |
| Argo CD | https://localhost:8083 |

Admin password: `kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath='{.data.password}' \| base64 -d`

## One-shot rerun

```bash
./scripts/15-e2e-deploy.sh
```
