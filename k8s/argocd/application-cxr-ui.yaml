# SW.8 — Argo CD Application (requires Git repo with this chart).
# Set CXR_ARGO_REPO_URL before apply, e.g.:
#   export CXR_ARGO_REPO_URL=https://github.com/UdonsiKalu/cxr-ops-lab.git
#   export CXR_ARGO_REPO_REVISION=main
#   envsubst < k8s/argocd/application-cxr-ui.yaml | kubectl apply -f -
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cxr-ui
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: ${CXR_ARGO_REPO_URL}
    targetRevision: ${CXR_ARGO_REPO_REVISION}
    path: helm/cxr-ui
    helm:
      valueFiles:
        - values.yaml
      # GIT-001: do not pin live Helm parameters here. Edit helm/cxr-ui/values.yaml,
      # push to main, let Argo sync. Live kubectl patches create Git drift.
      # GATE-002 winner: autoscaling.maxReplicas = 4 (see values.yaml).
  destination:
    server: https://kubernetes.default.svc
    namespace: cxr-ui
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
