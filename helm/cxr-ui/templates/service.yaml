apiVersion: v1
kind: Service
metadata:
  name: cxr-ui
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: cxr-ui
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: ClusterIP
  selector:
    app: cxr-ui
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
