# kind cluster for cxr-lab (Linux: pods reach host SQL/Qdrant via gateway IP in Helm).
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: cxr-lab
networking:
  apiServerAddress: "127.0.0.1"
nodes:
  - role: control-plane
